{
  "name": "kiivi_web",
  "version": "1.0.0",
  "description": "## Run locally",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "localize": "static-i18n -l en -i en -i fi -i sv -i et --fileFormat yaml --allowHtml true --outputDir . templates"
  },
  "author": "Joris Demange",
  "license": "ISC",
  "devDependencies": {
    "static-i18n": "^0.2.10"
  }
}
